# 本地 dev / preview 固定端口 5164（vite.config.js 的 server.port / preview.port，strictPort=true）
# 端口被占用时直接报错退出，不得静默回退到 5173 等默认端口（附录 B-B4）

# 数据源单一开关（spec §8.3-1：DATA_SOURCE=mock|api）
# 浏览器端必须带 VITE_ 前缀才能注入；代码内统一读作 DATA_SOURCE（见 src/config/env.js）
VITE_DATA_SOURCE=mock
# 真实实现的 API 前缀（下轮接真库时使用；mock 下不请求）
VITE_API_BASE_URL=/api/v1
